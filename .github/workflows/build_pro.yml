name: 🚀 LiquidSnap Zero-Error Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # زر التشغيل اليدوي

jobs:
  build_apk:
    name: 🏗️ Build Android APK
    runs-on: ubuntu-latest

    steps:
      # 1. سحب الكود
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      # 2. إعداد جافا (بدون كاش لتجنب أخطاء المسارات)
      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          # تم إزالة سطر cache: 'gradle' لأنه سبب المشكلة السابقة

      # 3. إعداد فلاتر (النسخة المستقرة)
      - name: ⚡ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.19.0' # تثبيت نسخة محددة لضمان الاستقرار

      # 4. التأكد من وجود مجلد التطبيق (فحص أمان)
      - name: 🔍 Verify Frontend Directory
        run: |
          if [ ! -d "frontend" ]; then
            echo "❌ Error: Folder 'frontend' not found!"
            exit 1
          fi
          echo "✅ Frontend folder found."

      # 5. تحميل المكتبات (داخل مجلد frontend حصراً)
      - name: 📦 Install Dependencies
        working-directory: ./frontend
        run: flutter pub get

      # 6. بناء ملف APK (الأمر المباشر)
      - name: 🛠️ Build APK (Release)
        working-directory: ./frontend
        run: flutter build apk --release

      # 7. الرفع النهائي (مسار الملف مضمون)
      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LiquidSnap-App
          path: frontend/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 5

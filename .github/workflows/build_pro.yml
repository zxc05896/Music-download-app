name: 🚀 LiquidSnap Root Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build_apk:
    name: 🏗️ Build Android APK
    runs-on: ubuntu-latest

    steps:
      # 1. سحب الكود
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      # 2. إعداد جافا 17
      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 3. إعداد فلاتر
      - name: ⚡ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.19.0'

      # 4. تنظيف وتثبيت المكتبات
      # (تم إزالة working-directory لأننا في الجذر)
      - name: 📦 Install & Clean Dependencies
        run: |
          flutter clean
          flutter pub get

      # 5. بناء التطبيق
      - name: 🛠️ Build APK (Release)
        run: flutter build apk --release --no-tree-shake-icons

      # 6. الرفع النهائي
      # (لاحظ تغير المسار: شلنا كلمة frontend من الأول)
      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LiquidSnap-App-2026
          path: build/app/outputs/flutter-apk/app-release.apk

buildscript {
    // 🚀 Kotlin 2.3.10: أحدث إصدار نزل يوم 5 فبراير 2026
    ext.kotlin_version = '2.3.10'
    
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        // 🛠️ AGP 8.9.0: الإصدار المتوافق مع Kotlin 2.3+ في 2026
        classpath 'com.android.tools.build:gradle:8.9.0'
        
        // الكوتلين الحديث (K2 Compiler Enabled by default)
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

// 🔥 المنطقة المحظورة (The Fix Zone - 2026 Edition) 🔥
// الكود ده هيحل مشكلة connectivity_plus وكل المكتبات القديمة
// عن طريق إجبارها تستخدم Android 15 (API 35)
subprojects {
    afterEvaluate { project ->
        if (project.hasProperty("android")) {
            project.android {
                // 1. حل مشكلة الـ Namespace
                if (namespace == null) {
                    namespace project.group
                }
                
                // 2. إجبار استخدام Android 15 (API 35)
                compileSdk 35
                
                // 3. توحيد أدوات البناء
                buildToolsVersion "35.0.0"
                
                // 4. رفع الحد الأدنى للمكتبات ليتوافق مع أمان 2026
                if (project.android.hasProperty("defaultConfig")) {
                    defaultConfig {
                        minSdk 23 
                        targetSdk 35
                    }
                }
            }
        }
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

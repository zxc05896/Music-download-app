name: liquid_snap_glass
description: The Ultimate High-Performance Liquid Glass Video Downloader.
publish_to: 'none' 
version: 2.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🎨 1. UI & Visuals (The Matte Glass Engine)
  # القوة الضاربة للتصميم الزجاجي والأنيميشن
  glass_kit: ^3.0.0            # تأثير الزجاج المتقدم
  glassmorphism: ^3.0.0        # بديل قوي لعمل تأثيرات الثلج
  flutter_animate: ^4.5.0      # تحريك العناصر بدقة عالية
  animations: ^2.0.8           # حركات الانتقال الرسمية من جوجل
  lottie: ^3.1.0               # تشغيل ملفات الأنيميشن المعقدة (JSON)
  flutter_svg: ^2.0.10         # دعم الأيقونات الفيكتور عالية الجودة
  google_fonts: ^6.1.0         # خطوط احترافية (Inter, Audiowide)
  shimmer: ^3.0.0              # تأثير اللمعان أثناء التحميل
  cached_network_image: ^3.3.1 # تخزين الصور عشان التطبيق يبقى طيارة

  # 🌐 2. Networking & Download Core (The Beast)
  # محرك الاتصال والتحميل
  http: ^1.2.0                 # الاتصال الأساسي
  dio: ^5.4.0                  # عملاق التحميل (يدعم الاستكمال والسرعة)
  connectivity_plus: ^6.0.0    # فحص حالة الإنترنت
  url_launcher: ^6.2.4         # فتح الروابط الخارجية

  # ⚙️ 3. State Management & Logic
  # العقل المدبر
  provider: ^6.1.1             # إدارة الحالة (خفيف وسريع)
  permission_handler: ^11.3.0  # إدارة الصلاحيات المعقدة (أندرويد 13+)
  path_provider: ^2.1.2        # الوصول لمسارات النظام
  device_info_plus: ^10.1.0    # معرفة نوع الجهاز لضبط الإعدادات

  # 💾 4. File Handling & System
  # التعامل مع الملفات والإشعارات
  open_filex: ^4.3.2           # فتح الملفات بعد تحميلها
  flutter_local_notifications: ^17.0.0 # إشعارات عند اكتمال التحميل
  gallery_saver: ^2.3.2        # حفظ الفيديو في الاستوديو مباشرة
  flutter_plugin_android_lifecycle: ^2.0.17 # إصلاح مشاكل الذاكرة

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8         # لتوليد الأكواد أوتوماتيكياً (لو احتجنا)

flutter:
  uses-material-design: true

  # 🖼️ Assets Configuration
  # تأكد من إنشاء مجلد assets و images داخله
  assets:
    - assets/images/
    - assets/icons/

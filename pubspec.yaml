name: liquid_snap_glass
description: The Ultimate High-Performance Liquid Glass Video Downloader.
publish_to: 'none' 
version: 2.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🎨 1. UI & Visuals (The Matte Glass Engine)
  glass_kit: any
  glassmorphism: any
  flutter_animate: any
  animations: any
  lottie: any
  flutter_svg: any
  google_fonts: any
  shimmer: any
  cached_network_image: any

  # 🌐 2. Networking & Download Core
  http: any
  dio: any
  connectivity_plus: any
  url_launcher: any

  # ⚙️ 3. State Management & Logic
  provider: any
  permission_handler: any
  path_provider: any
  device_info_plus: any

  # 💾 4. File Handling & System
  open_filex: any
  flutter_local_notifications: any
  gallery_saver: any
  flutter_plugin_android_lifecycle: any

# 🔥 حلال المشاكل: هذا الجزء يجبر كل المكتبات على استخدام النسخة الأحدث
# ويحل مشكلة الـ Conflict التي ظهرت لك في الـ Actions
dependency_overrides:
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any
  build_runner: any

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
